name: vaiprasala
description: "A new Flutter project."
publish_to: 'none'
version: 0.1.0

environment:
  sdk: ^3.6.2

dependencies:
  flutter:
    sdk: flutter
  shared_preferences: ^2.5.2
  supabase_flutter: ^2.8.4
  app_links: ^3.4.0
  flutter_launcher_icons: ^0.14.3

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true
  assets:
    - images/logo.png
    - images/logo_verde.png

flutter_icons:
  android: true
  ios: true
  image_path: "images/sologo.png"